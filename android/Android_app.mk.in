# Template for Android.mk for VSM android target.

include $(CLEAR_VARS)

LOCAL_MODULE    := vsm-sdk

LOCAL_SRC_FILES := @VSM_SDK_DIR@/android/lib/$(TARGET_ARCH_ABI)/libvsm-sdk.a

include $(PREBUILT_STATIC_LIBRARY)

include $(CLEAR_VARS)

LOCAL_MODULE     := @CMAKE_PROJECT_NAME@

LOCAL_C_INCLUDES := @ANDROID_INCLUDE_DIRS@

LOCAL_CFLAGS := @ANDROID_CFLAGS@

LOCAL_SRC_FILES  := @ANDROID_SOURCES@

LOCAL_STATIC_LIBRARIES := vsm-sdk

LOCAL_LDLIBS := -llog -latomic

include $(BUILD_SHARED_LIBRARY)
